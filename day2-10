THREE IDIOTS
#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {

    int x1,x2,y1,y2;
    float x,y;
    scanf("%d %d %d %d",&x1,&x2,&y1,&y2);
    x= (float)(x1+y1)/2;
    y= (float)(x2+y2)/2;
    printf("Binoy's house is located at (%.1f,%.1f)",x,y);
}
